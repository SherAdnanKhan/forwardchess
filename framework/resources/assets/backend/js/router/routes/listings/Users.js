import {USERS_PATH} from '../../paths';
import {USERS_NAME} from '../../names';
import Users from '../../../components/pages/listings/Users';

export default {
    component: Users,
    name     : USERS_NAME,
    path     : USERS_PATH
};
